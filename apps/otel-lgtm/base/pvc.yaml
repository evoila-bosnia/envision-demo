apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: otel-lgtm-data
  labels: { app: otel-lgtm }
spec:
  accessModes: [ ReadWriteOnce ]
  resources:
    requests:
      storage: 10Gi
  # storageClassName is injected by overlays/envision (the NFS-SPBM StorageClass).
