apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# App bundle = the base + the shared ENVISION component (storageclass + LB knobs).
resources:
  - ../../base
components:
  - ../../../../overlays/envision
